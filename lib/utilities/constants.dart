import 'package:flutter/material.dart';

const kBookMarkIcon = Icon(
  Icons.bookmark,
  color: Colors.white,
);
const kSendIcon = Icon(
  Icons.send,
  color: Colors.white,
);
const kHeartIcon = Icon(
  Icons.heart_broken,
  color: Colors.white,
);
const kCommentIcon = Icon(
  Icons.comment,
  color: Colors.white,
);
const kPostBoxDecoration = BoxDecoration(
  color: Color.fromARGB(255, 231, 238, 250),
  borderRadius: BorderRadius.all(
    Radius.circular(60),
  ),
);
